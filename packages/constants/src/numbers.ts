/** Default pagination page size. */
export const PAGE_SIZE = 10 as const

/** Default debounce delay in milliseconds. */
export const DEBOUNCE_MS = 300 as const

/** Default toast display duration in milliseconds. */
export const TOAST_DURATION = 5000 as const

/** Mobile breakpoint threshold in pixels. */
export const MOBILE_BREAKPOINT = 850 as const

/** Maximum file upload size in bytes (1 MB). */
export const MAX_FILE_SIZE = 1_048_576 as const

/** Minimum password length requirement. */
export const MIN_PASSWORD_LENGTH = 8 as const
