/** @public — Prisma client singleton with PrismaPg adapter for Neon/PostgreSQL */
export { db } from "./client";

/** @public — Re-export of PrismaClient type for consumer use */
export type { PrismaClient } from "./generated/client";
