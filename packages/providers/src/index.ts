/**
 * Flattens a ProviderTree into a single React element.
 * Providers are composed left-to-right (outermost to innermost).
 * @public
 */
export {
  ProviderComposer,
  type ProviderComposerProps,
  type ProviderProps,
  type ProviderTree,
} from './provider-composer';
